drop table if exists member CASCADE;
create table member
(
    id  bigint generated by default as identity,

    userId varchar(255),

    userPw varchar(255),

    name varchar(255),

    primary key (id)
);